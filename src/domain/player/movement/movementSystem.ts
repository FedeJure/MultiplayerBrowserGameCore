import { PlayerInput } from "../playerInput";
import { Player } from "../players/player";

export class MovementSystem {
    processMovement(player: Player, input: PlayerInput) {
        
    }
}